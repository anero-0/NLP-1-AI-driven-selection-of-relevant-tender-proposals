version: "3.9"

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # Монтируем папку models чтобы можно было обновлять модель без пересборки образа
      - ./models:/app/models:ro
    restart: unless-stopped
